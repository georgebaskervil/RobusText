#ifndef SDL_WINDOW_H
#define SDL_WINDOW_H

// Displays the given UTF-8 text in an SDL window using the specified font and size.
void display_text_window(const char *utf8_text, const char *font_path, int font_size);

#endif // SDL_WINDOW_H